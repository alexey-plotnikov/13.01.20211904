import React from "react";

import "./ProfileSection.scss";

const ProfileSection = (props) => {
  const {} = props;

  return (
    <div className="profile-section-container">
      ProfileSection works
    </div>
  );
};

export default ProfileSection;